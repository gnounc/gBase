void checkMultitrigger(entity p)
{	
	if (self.classname == "trigger_secret")
	{
		if(total_secrets == found_secrets)
		{

		}
//			markCheevo(p, ACHV_SNOWDEN);
	}

	if (strtolower(self.message) == "the dopefish lives!")
	{
		
	}
//		markCheevo(p, ACHV_DOPEFISH);
}
