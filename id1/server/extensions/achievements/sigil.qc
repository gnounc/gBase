void checkSigil(entity p)
{
//CHECK ME. . . probably fucked --gnounc
	if((serverflags == serverflags | (1 & 15)))
		if((serverflags == serverflags | (2 & 15)))
			if((serverflags == serverflags | (4 & 15)))
				if((serverflags == serverflags | (8 & 15)))
				{
					
				}
//					markCheevo(p, ACHV_GIANT);

}

