//gnounc		--pre healed values, you can check player stats and healthpack stats 
//				before any historic values are lost. (you can know how much the player healed for)
void checkHeal(entity p)
{
	if(p.health == 1)
		if(self.healamount == 100)
		{
			
		}
//			markCheevo(p, ACHV_HEALING101);
}